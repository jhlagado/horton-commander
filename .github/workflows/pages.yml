name: Horton preview site
on:
  push:
    branches: [main]
    paths: ["site/**", ".github/workflows/pages.yml"]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: horton-pages
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      - name: Verify published disk images
        run: |
          python3 - <<'PYTHON'
          import hashlib, json, pathlib
          for descriptor in pathlib.Path('site/releases').glob('*/system.json'):
              record = json.loads(descriptor.read_text())
              assert record['schema'] == 'triptych-external-system-v1'
              assert record.get('workDrives', ['B']) == ['B', 'C', 'D']
              image = (descriptor.parent / record['image']['asset']).read_bytes()
              assert len(image) == record['image']['bytes']
              assert hashlib.sha256(image).hexdigest() == record['image']['sha256']
          PYTHON
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9
        with:
          path: site
      - uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346
        id: deployment
